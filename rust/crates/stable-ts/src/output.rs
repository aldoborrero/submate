//! Stage D — SRT/VTT output formatting (`to_srt_vtt`, timestamp formatting,
//! word-level highlight tags).
//!
//! Stub — implemented by the grind backlog item `port-stablets-output-D`,
//! which fills THIS file only (the module is pre-declared in `lib.rs` to avoid
//! crate-root merge contention with the other stable-ts stages).
