//! `submate` binary — clap CLI (ports submate/cli/).
//!
//! Stub — implemented by the grind backlog item `port-cli-commands`.

fn main() {
    eprintln!("submate: not yet implemented (Rust port in progress)");
    std::process::exit(1);
}
